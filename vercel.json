{
  "builds": [
    {
      "use": "@vercel/python",
      "entrypoint": "python server.py",
      "config": {
        "python": "python3.10",
        "buildCommand": "source bin/activate && pip install -r requirement.txt && python server.py"
      }
    }],
  "routes": [{ "src": "/(.*)", "dest": "/server.py" }]
}
